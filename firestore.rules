rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DSPL Manufacturing MIS collections:
    // Allow authenticated users to read and write records.
    // Allow read/write for manufacturing operations while preserving data integrity.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
